# JobsGeTelegramBot

**Moved.** This bot now lives inside
[DarkLordGeo/scrap_jobs_ge](https://github.com/DarkLordGeo/scrap_jobs_ge/tree/claude/review-8a0oz2/telegram_bot),
next to the scraper whose data it reads — so it can read
`database/jobs_data.json` directly instead of checking it out from another
repo. See that repo's `telegram_bot/` folder and
`.github/workflows/telegram_notify.yml` for the current version.

This repo is kept only so the link above doesn't break; it's not
maintained separately.
